# Python
__pycache__/
*.pyc
.venv/
venv/

# GCP credentials — never commit these
credentials/
*-key.json
service-account*.json
*credentials*.json

# Environment
.env
.env.*

# OS
.DS_Store

# Material de aula (não é documentação do projeto)
docs/FIAP_Fase2_DataPrepare_Transcricoes.txt

# Documentação interna de planejamento (specs, plans, PDF do desafio, roteiro do
# vídeo) — mantida só localmente, não faz parte do repositório público
docs/
